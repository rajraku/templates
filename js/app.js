var angularApp = angular.module('templates', []);

angularApp
.register
.controllers('MyDetailsController', ['$rootScope', '$scope', function($rootScope, $scope){

	$scope.onSearch = function(value)
	{

	}

	
});